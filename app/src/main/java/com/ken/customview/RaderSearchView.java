package com.ken.customview;

import android.content.Context;
import android.support.annotation.Nullable;
import android.util.AttributeSet;
import android.view.View;

public class RaderSearchView extends View {
    public RaderSearchView(Context context) {
        super(context);
    }

    public RaderSearchView(Context context, @Nullable AttributeSet attrs) {
        super(context, attrs);
    }
}
